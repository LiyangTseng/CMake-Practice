# specify the minimum required cmake version
cmake_minimum_required(VERSION 2.6)

# set project name 
project(trial_project)

# specify cpp file(main.cpp) and its compiled executable file (main.o)
add_executable(main.o main.cpp)